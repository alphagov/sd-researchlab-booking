import React from 'react';
import { withRouter } from 'react-router-dom';

const BookingFormDetails = ({ history }) => {
  return <div>Booking form details</div>;
};

export default withRouter(BookingFormDetails);
